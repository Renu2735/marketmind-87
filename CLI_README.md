# MarketAI Suite - CLI Quick Start Guide

## 🚀 Run Without Browser

Use this command to run MarketAI Suite directly in Command Prompt:

```cmd
python cli.py
```

## 📋 Features Available in CLI Mode

1. **Generate Ad Campaigns** - Create marketing campaigns
2. **Create Sales Pitches** - Generate compelling pitches  
3. **Score Leads** - Evaluate lead quality
4. **View Saved Items** - Access all saved generations

## 💾 Data Storage

All generated content is automatically saved to JSON files:
- `saved_campaigns.json` - All campaigns
- `saved_pitches.json` - All pitches
- `saved_leads.json` - All lead scores

## ⚡ Quick Commands

```cmd
# Navigate to project
cd c:\MARKETMIND-87

# Run CLI version (no browser needed)
python cli.py

# Run web version (with browser)
python app.py
```

## 🎯 Usage Example

```
1. Run: python cli.py
2. Select option: 1 (Generate Campaign)
3. Enter product details
4. Get AI-generated campaign
5. Save to file
```

That's it! No browser required! 🎉
